-- CLEANUP
for _, old in pairs(game.CoreGui:GetChildren()) do
    if old.Name == "SurvivorHub" then old:Destroy() end
end

_G.BotRunning = true
local player = game.Players.LocalPlayer
local Pathfinding = game:GetService("PathfindingService")

local lastWander = 0
local wanderTarget = nil

-- GUI
local sg = Instance.new("ScreenGui", game.CoreGui)
sg.Name = "SurvivorHub"
local frame = Instance.new("Frame", sg)
frame.Size = UDim2.new(0, 220, 0, 90)
frame.Position = UDim2.new(0.5, -110, 0.05, 0)
frame.BackgroundColor3 = Color3.fromRGB(15, 20, 30)
Instance.new("UICorner", frame)

local status = Instance.new("TextLabel", frame)
status.Size = UDim2.new(1, 0, 1, 0)
status.TextColor3 = Color3.new(1, 1, 1)
status.BackgroundTransparency = 1
status.Text = "SMART AI RUNNING"
status.Font = Enum.Font.GothamBold

-- 1. АНТИ-АФК И РЕДЖОИН
player.Idled:Connect(function()
    game:GetService("VirtualUser"):CaptureController()
    game:GetService("VirtualUser"):ClickButton2(Vector2.new(0,0))
end)

game:GetService("GuiService").ErrorMessageChanged:Connect(function()
    task.wait(5)
    game:GetService("TeleportService"):Teleport(game.PlaceId, player)
end)

-- УМНАЯ ХОДЬБА ЧЕРЕЗ PATHFINDING (Для Win и важных целей)
local function walkPath(targetPos)
    local char = player.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not hum or not root then return end

    local path = Pathfinding:CreatePath({
        AgentRadius = 2.5,
        AgentCanJump = true,
        WaypointSpacing = 4
    })
    
    local success, _ = pcall(function() path:ComputeAsync(root.Position, targetPos) end)
    
    if success and path.Status == Enum.PathStatus.Success then
        local wps = path:GetWaypoints()
        -- Берем первые несколько точек, чтобы бот двигался плавно и без лагов
        for i = 1, math.min(#wps, 4) do
            if not _G.BotRunning or hum.Health <= 0 then break end
            hum:MoveTo(wps[i].Position)
            if wps[i].Action == Enum.PathWaypointAction.Jump then hum.Jump = true end
            
            local arrived = hum.MoveToFinished:Wait(0.3)
            if not arrived and root.AssemblyLinearVelocity.Magnitude < 3 then 
                hum.Jump = true 
                break 
            end
        end
    else
        -- Напролом, если путь заблокирован намертво
        hum:MoveTo(targetPos)
        if root.AssemblyLinearVelocity.Magnitude < 3 then hum.Jump = true end
    end
end

-- ОБЫЧНЫЙ СТАРЫЙ ХОД (Для кнопок)
local function walkDirect(targetPos)
    local char = player.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not hum or not root then return end

    hum:MoveTo(targetPos)
    if root.AssemblyLinearVelocity.Magnitude < 3 then hum.Jump = true end
end

-- ФУНКЦИЯ ДЛЯ УМНОГО WANDERING (Сканирование стен лучами)
local function getSafeWanderPos(root)
    local directions = {
        Vector3.new(1, 0, 0),   -- Право
        Vector3.new(-1, 0, 0),  -- Лево
        Vector3.new(0, 0, 1),   -- Назад
        Vector3.new(0, 0, -1),  -- Вперед
        Vector3.new(1, 0, 1).Unit,
        Vector3.new(-1, 0, -1).Unit
    }
    
    -- Перемешиваем направления, чтобы ходить случайно
    for i = #directions, 2, -1 do
        local j = math.random(i)
        directions[i], directions[j] = directions[j], directions[i]
    end

    -- Проверяем лучами, куда идти безопасно
    for _, dir in ipairs(directions) do
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Exclude
        raycastParams.FilterDescendantsInstances = {player.Character}

        -- Стреляем лучом на 25 блоков вперед в этом направлении
        local rayResult = workspace:Raycast(root.Position, dir * 25, raycastParams)
        
        -- Если луч ни обо что не ударился — это направление свободное и безопасное!
        if not rayResult then
            return root.Position + (dir * math.random(15, 25))
        end
    end
    
    -- Если везде стены, просто выдаем случайную точку как запасной вариант
    return root.Position + Vector3.new(math.random(-20, 20), 0, math.random(-20, 20))
end


-- ГЛАВНЫЙ ИСПОЛНИТЕЛЬНЫЙ ЦИКЛ
task.spawn(function()
    while _G.BotRunning do
        task.wait(0.15)
        pcall(function()
            local char = player.Character
            local root = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChildOfClass("Humanoid")
            local disaster = workspace:FindFirstChild("Disaster")

            if not root or not hum or hum.Health <= 0 then return end

            -- ОБЪЕКТЫ
            local water = disaster and disaster:FindFirstChild("Water", true)
            local win = disaster and disaster:FindFirstChild("Win", true)
            local tableBtn = disaster and disaster:FindFirstChild("TableNButtonClickDetector", true)
            local CLICKButton = workspace:FindFirstChild("CLICKButton", true)
            local weapon = char:FindFirstChild("ClassicSword") or player.Backpack:FindFirstChild("ClassicSword") or 
                           char:FindFirstChild("RocketLauncher") or player.Backpack:FindFirstChild("RocketLauncher")

            -- ЛОГИКА ПРИОРИТЕТОВ
            if water then
                status.Text = "⚡ TP: WATER EVASION"
                local high = root.Position
                for _, p in pairs(disaster:GetDescendants()) do
                    if p:IsA("BasePart") and p.CanCollide and p.Position.Y > high.Y and p.Name ~= "Water" then 
                        high = p.Position 
                    end
                end
                root.CFrame = CFrame.new(high + Vector3.new(0, 5, 0))

            elseif weapon then
                status.Text = "🚀 HUNTING PLAYERS"
                if weapon.Parent == player.Backpack then hum:EquipTool(weapon) end
                local target = nil
                local dist = 1000
                for _, p in pairs(game.Players:GetPlayers()) do
                    if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
                        local d = (root.Position - p.Character.HumanoidRootPart.Position).Magnitude
                        if d < dist then dist = d target = p.Character.HumanoidRootPart end
                    end
                end
                if target then
                    walkDirect(target.Position)
                    root.CFrame = CFrame.new(root.Position, Vector3.new(target.Position.X, root.Position.Y, target.Position.Z))
                    weapon:Activate()
                end

            elseif tableBtn then
                status.Text = "🔘 CLICKING TABLE"
                walkDirect(tableBtn.Parent.Position)
                if (root.Position - tableBtn.Parent.Position).Magnitude < 10 then 
                    fireclickdetector(tableBtn) 
                end

            elseif win then
                status.Text = "🏆 PATHFINDING TO WIN"
                -- ЗДЕСЬ СТРОГО УМНЫЙ PATHFINDING
                walkPath(win.Position)

            elseif CLICKButton then
                status.Text = "🎮 GOING TO START"
                local pos = CLICKButton:IsA("BasePart") and CLICKButton.Position or CLICKButton.Parent.Position
                walkDirect(pos)
                if (root.Position - pos).Magnitude < 10 then
                    fireclickdetector(CLICKButton:FindFirstChildOfClass("ClickDetector") or CLICKButton)
                end
            else
                -- УМНЫЙ IDLE БЕЗ СТЕН
                status.Text = "🚶 SMART WANDERING..."
                if tick() - lastWander > 3 or not wanderTarget then
                    lastWander = tick()
                    wanderTarget = getSafeWanderPos(root) -- Получаем безопасную точку лучами
                end
                walkDirect(wanderTarget)
            end
        end)
    end
end)
